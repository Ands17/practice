
//anchor: devuelve el texto dentro de una etiqueta a (enlace)

//big: devuelve el texto formato mas grande

//bold:devuelve texto negrita
//charat:nos da el caracter que coincida con el numero que pasemos por argumento
//charCodeArt: devuelve el unicode que coincida con el numero que le pasemos por el argumentos

//concat:une string y/o variable

//endswidth:nos devuelve true o false al caracter que le indicquemos .

//fontcolor:cambia color del string
//fontsize:cambia el tamaño del estring pasando por el argumento el numero

//includes: devuelve true o false en funcion del caracter que le indiquemos esta en el texto

//indexoff:si existe el indice que le indiquemos en el argumento nos da su posicion
//sini esta -1


//link: devuelve string en link y si pasas por el argumento la pagina web

//match: nos devuelve la coincidencia si existe
//sino devuelve null

//repeat: nos repite las veces que le indiquemos por el argumento

//replace: permite reemplazra una parte del estring por otro
//pasando por primer argumentp la frase que queremos cambiar separada por coma(,)
//y por un segundo argumento la nueva 

//slice: devuelve una porcion de cadena que le indiquemos por 2 numeros


//split: nos divide el string en substring
//console.log(frase.split(" ",10))

//starwidth: nos da true o flase en caracter a la letra que paemos por el argumento, distingue entre minusculas.

//sub: nos da destring como si fuera subtitulo
//frase.sub()

//substring: devuelve una porcion en cadena entre dos numeros que le pasemos como argumento incluyemdo el premero pero no el segund0
// frase.substrin(5,8)

//ToLocaleLowercase: convierte en minuscula
//frase.toLocaleLowerCase()

//TolocaleUpperCAse: Convierte en mayusculas

//Tolowercase: convierte cadena a minuscula
//toupperCase
